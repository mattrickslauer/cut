# Python
__pycache__/
*.py[cod]
.venv*/
venv/
*.egg-info/

# Render outputs / recordings (ephemeral — belong in object storage, not git)
*.mp4
*.webm
*.wav
*.mov
out/
renders/
.data/

# Node
node_modules/
dist/

# Secrets — ignore every .env variant, but keep the committed template
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

# Claude Code session data / worktrees — never publish
.claude/
